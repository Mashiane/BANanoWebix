B4J=true
Group=Default Group
ModulesStructureVersion=1
Type=Class
Version=7.51
@EndOfDesignText@
Sub Class_Globals
	Public line As String = "line"  ' Cells with borders (this Is the default)
	Public clean As String = "clean" 'Cells without borders
	Public wide As String = "wide" 'Cells with borders And extra-wide space between siblings
	Public space As String = "space" 'Cells with borders And extra-wide space between siblings, plus padding from the parent container
	Public head As String = "head" 'Cells with borders And small space between siblings
	Public form As String = "form" 'Cells without borders And with padding around all of them
End Sub

'Initializes the object. You can add parameters to this method if needed.
Public Sub Initialize
	
End Sub